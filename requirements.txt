# Core dependencies - pinned to stable versions for Streamlit Cloud compatibility
streamlit==1.35.0
plotly==5.18.0
pandas==2.0.3
numpy==1.24.3

# Google Trends API
pytrends==4.9.2

# Mapping (optional - used in DMA Deep Dive)
folium==0.15.1
streamlit-folium==0.18.0

# AI Chat (optional - requires API key)
anthropic==0.7.8
